app = "corredor-worker-prod"
primary_region = "gru"

[build]
  dockerfile = "apps/worker/Dockerfile"

[env]
  NODE_ENV = "production"

# 2 Playwright/Chromium instances need ~450 MB each; 1024 MB gives headroom.
[[vm]]
  size = "shared-cpu-2x"
  memory = "1024mb"
